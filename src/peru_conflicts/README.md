# Package layout

This package contains strict configuration/path safety, hashing and stable IDs, versioned source-oriented models, schema export, run metadata, structured logging, and repository policy checks. Corpus parsers are intentionally absent during Milestone 0.
