---
name: extract-report
description: Extract one explicitly authorized benchmark report into source-preserving structured artifacts after the relevant Milestone 2 or 3 gate is active.
---

# Extract one report

Confirm milestone and report scope. Verify source hash, select an evidence-based regime, extract native/layout/table evidence deterministically, segment source units, validate strict schemas, and retain field-level provenance. Model-assisted work is opt-in, segmented, cached, schema-constrained, source-grounded, and may return null. Write only to derived storage. Stop at report QA; never scale to the corpus.
