# Generated schemas

JSON Schemas are generated from `peru_conflicts.models.MODEL_REGISTRY` by `scripts/export_schemas.py`. Each schema version has an immutable directory, currently `v0.1.0/`. Regenerate only the current version and preserve prior version directories. Python/Pydantic validation remains authoritative for semantic cross-field invariants documented in `docs/07_canonical_data_model.md`.
