# Generated schemas

JSON Schemas are generated from `peru_conflicts.models.MODEL_REGISTRY` by `scripts/export_schemas.py`. The current source contract is `v0.2.0/`; `v0.1.0/` is the immutable M0 snapshot. Regenerate only the current version and preserve prior version directories. Python/Pydantic validation remains authoritative for semantic cross-field invariants documented in `docs/07_canonical_data_model.md`.
