# M2-02B.1b Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Do not stage, commit or push; the primary agent makes one final reviewed commit.

**Goal:** Correct access integrity and Python environment identity without granting launch authority.
**Architecture:** A versioned typed access policy produces real-test templates. A deterministic Python-environment manifest extends existing runtime custody; independent provisioning is distinct from in-process consistency. Existing neutral scientific logic remains shared.
**Tech Stack:** Python 3.12-3.13, Pydantic, pytest, Ruff, Pyright, uv.
**Spec:** docs/superpowers/specs/2026-09-14-m2-02b1b-hardening-design.md

## Global Constraints

- Base head 109c40be0aac44d87d6e0e5471cecb4e57ed84c2; same branch and PR #16; main 995b54115f94454fc81d4af36f138add43ba8614 unchanged.
- No frozen schema/metric/evaluator/owner-readiness/reference/original-package changes.
- All16 launch responses null; all real account checks NOT RUN; all operational flags false.
- No Dropbox write, production provisioning, real humans/issuance/annotation/lock/gold/parser/M3/acquisition.
- Preserve historical v1 access semantics and all old evidence. New ignored artifacts only under .cache/m2-02b1b.
- User approved private account-scoped read/write drafts; own issue read/list only; foreign/coordinator list/read/write denied. Coordinator operational writes allowed, immutable acceptance application-controlled.
- TDD, no safety-test deletion/skips. One implementer at a time, independent scoped review after each task.

### Task 1: Versioned least-privilege access policy

**Files:** Create src/peru_conflicts/execution/access_policy.py and tests/unit/test_m2_access_policy.py. Modify launch.py, launch_review.py, relevant launch tests, draft candidate access version and access sections of launch protocol/coordinator checklist.
**Interfaces:** Preserve ACCESS_CHECKS_V1 as immutable historical content. Export typed AccessExpectation/AccessPolicy and ACCESS_POLICY_V2. Expose mechanically derived ACCESS_CHECKS compatible with current receipt iteration; expose access-policy identity/version to review evidence. Task2 consumes LaunchIdentity without changing policy decisions.

- [ ] Reproduce current defect with a failing assertion before code changes:
  ```python
  assert any(
      actor == "annotator-a"
      and op == "write"
      and resource == "annotator-a/issue"
      and outcome == "deny"
      for actor, op, resource, outcome in ACCESS_CHECKS.values()
  )
  ```
  Also assert denied writes for both roles to foreign issue/submission and each coordinator area. Save exact RED output.
- [ ] Add strict typed rows with actor/resource/list-read-write semantics, uppercase outcomes, control layer, rationale and unique IDs; derive all required ACL tests. Keep application immutability rules separately typed, not substitutes for ACL denials.
- [ ] Version v2, preserve v1, update real NOT RUN receipt validation and synthetic completeness to require exactly derived rows. Reject missing write-denial rows, duplicates, wrong expected outcomes and real PASS/FAIL. Keep private draft normal read/write semantics explicit.
- [ ] Run new policy, launch-preflight and launch-review tests; update assertions that pinned old54 count to derivation while retaining v1 historical54 proof. Test all expected actors/resources/operations independently, not only row count.
- [ ] Update decision6 evidence descriptions and topology/held-out descriptions only as required; all16 responses null. Document capability limits and coordinator hash-controlled accepted bytes. Report code diff and RED/GREEN commands for independent review.

### Task 2: Non-circular Python environment binding

**Files:** Create src/peru_conflicts/execution/python_environment.py and tests/unit/test_m2_python_environment.py. Modify runtime_build.py, runtime_cli.py, launch.py, relevant isolated-runtime/launch/review fixtures and tests, build script if needed, and Python trust sections of launch protocol.
**Interfaces:** Export strict PythonEnvironmentTrustManifest and deterministic rehearsal inventory/identity helpers; RuntimeManifest and LaunchIdentity carry the environment manifest/aggregate pins; both A/B pair checks require equality. Existing dependency aggregate stays independently pinned. Task3 uses build outputs to refresh only unapproved candidate/evidence.

- [ ] Capture RED for missing environment fields and equal interpreter/dependency identity across distinct stdlib inventories:
  ```python
  assert "python_environment_sha256" in LaunchIdentity.model_fields
  assert "python_environment_sha256" in RuntimeManifest.model_fields
  ```
  Add behavioral tests of stale/substituted manifest, stdlib bytes, aggregate, receipt and A/B environment mismatch before implementing each behavior.
- [ ] Define deterministic path-neutral inventory policy for supported Windows/Linux CPython installation runtime files: complete stdlib, applicable zip/extension/native distribution files; exclude caches/site-packages/user packages and explicitly name external OS trust boundary. Strictly validate safe relative paths, inventory completeness and self-consistency. Rehearsal capture is not independent production authentication.
- [ ] Extend runtime/candidate/rehearsal identity and separately supplied expected pins. Bind dependency, interpreter and stdlib identity consistently; reject mismatched copies. Preserve retained-file and no-repository-fallback guarantees. Do not authenticate a trust root solely with a Python process running on that root.
- [ ] Preserve -I -S -B, document independent pre-execution provisioning and in-process consistency separately. Real production use remains forbidden regardless of self-consistent rehearsal hashes. Avoid duplicate normative validation logic.
- [ ] Run Python environment, isolated runtime, launch and neutral tests, all existing mutation/custody cases. Record RED/GREEN, exact interfaces, supported installation scope and residual OS trust prerequisites for independent review.

### Task 3: Renew candidate, receipts and documentation

**Files:** Modify launch_review.py, scripts/prepare_m2_launch_review.py only if necessary, draft config/benchmark/m2_02_launch_candidate_v1.yaml, relevant launch-review tests and docs. Generate only .cache/m2-02b1b artifacts; preserve previous evidence.
**Interfaces:** Consume Tasks1/2 typed policy and environment pins. Produce immutable hardening packet with baseline/new identities, derived v2 NOT RUN checks and all16null dossier. Measured local/principal/CI evidence remains distinct from owner authority.

- [ ] Add negative tests before extending required hardening evidence: absent/stale environment or policy receipt must not yield completion.
- [ ] Build rehearsal environment/runtime manifests with current test distribution; refresh unapproved candidate and derived runtime receipt pins. Original package/reference/neutral-view bytes stay unchanged unless a necessary runtime-only projection update is explicitly explained.
- [ ] Generate initial incomplete evidence snapshot under .cache/m2-02b1b, binding old packet hashes and new runtime/policy/environment. Keep measured finalCI missing until actual new CI exists.
- [ ] Update owner decision6 evidence and environment provisioning launch prerequisite without adding/approving a decision. Maintain private human eligibility unresolved.
- [ ] Verify complete source/test quality, produce task report and reviewed diff.

### Task 4: Cleanup, final review, validation and same-PR closeout

**Files:** Ignored cleanup/audit/review/CI/final-report artifacts under .cache/m2-02b1b only, plus source corrections if principal review finds defects.
**Interfaces:** Preserve original main/PR baseline and authority hashes; review exact hardening diff. No new external operations beyond authorized normal push and PR metadata update.

- [ ] Inventory the exact two temp paths from prior evidence read-only, prove location/blankness/non-dependency, save pre-deletion hashes before any authorized removal. Uncertainty or policy denial is CLEANUP_DEFERRED_SAFELY; never bypass.
- [ ] Run all user-specified focused and full quality groups, with separate exact receipts and no new safety skips. Rehash frozen authority/Dropbox before and after.
- [ ] Principal review of hardening diff: access integrity, cross-role integrity, environment binding, no circularity, neutrality and all authority prohibitions. Fix Critical/Important findings through reviewed TDD; preserve findings history.
- [ ] Stage exact recipe paths; run actual staged-byte guard; create one second commit `Harden M2 launch isolation and runtime trust`, normal push, no merge. Renew PR16 description.
- [ ] Inspect new exact-head three-job CI/raw logs and verify feature tree equals merge-test tree. Finalize immutable packet with genuine CI/review evidence, all16null and real NOT RUN. Report artifact hashes, cleanup outcome, trust boundaries and exact final state.
