# M2-02A.1 Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans, with test-first implementation and an independent final review.

**Goal:** Implement a non-launched, fail-closed annotation execution candidate.

**Architecture:** Separate native reference custody, annotator-neutral packages, and coordinator-only submission lifecycle. Adapt human declarations to the existing benchmark without changing its registry or evaluator.

**Tech Stack:** Python 3.12–3.13, existing Pydantic/PyYAML, standard-library CLI/CSV, centrally pinned Poppler.

**Spec:** Jorge's M2-02A.1 instruction (2026-09-07), and the subsequently approved five-part design in this task.

## Global constraints

- Start at `8aad540557396b070caf143b2bd120940598a854` on `codex/m2-02a1-production-readiness`.
- Frozen benchmark/scientific schemas, approval records, evaluator and M3 policy stay byte-identical.
- No human annotation, gold, parser/scoring, OCR, acquisition or Dropbox writes.
- Real reference text and blank previews remain ignored repository cache only.
- Readiness and launch approvals remain false. PR creation 403 means manual handoff, not browser fallback.

## Execution checklist

### 1. Reference custody (`execution/references.py`, `test_m2_references.py`)

- [x] RED: source mismatch, page corruption/missing/duplicate, offset boundaries, native process failure.
- [x] Implement strict policy/page/manifest models, retained source reads, owned temporary snapshots, pinned extractor arguments, strict UTF-8 and per-page validation.
- [x] GREEN: fake process integration and deterministic synthetic pages; no network or external test files.

### 2. Neutral packages (`execution/packages.py`, `test_m2_packages.py`)

- [x] RED: partition/answer injection, incomplete references, mismatched package, non-neutral forms.
- [x] Implement exact file inventory, canonical manifests, blank CSV forms and source-position selection with no semantic suggestions.
- [x] GREEN: A/B equivalence except opaque identifiers and roles; structural integrity on load.

### 3. Human declarations and locks (`execution/annotation.py`, `test_m2_annotation.py`)

- [x] RED: incomplete inspection/slots, invalid evidence, early comparison, overwrite, altered locks and broken supersession.
- [x] Implement explicit family completeness, frozen benchmark adapters, separate validation/confirmation/lock, write-new publication, current-history checks and coordinator routing.
- [x] GREEN: synthetic different discoveries, cross-page boundaries, explicit zero/missing/ambiguous states, preserved disagreement and no gold.

### 4. CLI/config/docs (`scripts/prepare_m2_annotation.py`, readiness/run/reference configs)

- [x] Add human-operable CSV entry and line/column helper, validation and synthetic-only lifecycle APIs with actionable errors.
- [x] Document private eligibility, external isolation/sealing, immutable lock limitations, and separate future launch approval.
- [x] Validate all frozen authority and launch-false config states.

### 5. Real reference/blank preview and evidence

- [x] Compare native raw/layout structurally on all ten reports; select an explicitly draft recommendation.
- [x] Generate 1,128 references twice, require exact equality and source hashes/pages.
- [x] Produce neutral A/B previews, synthetic receipt, and fifteen-null-decision matrix in cache; final packet binds the later commit/PR outcome.

### 6. Review and release candidate

- [x] Full local quality matrix, schema drift/data policy, read-only Dropbox hash/inventory audit.
- [x] One independent principal code/preview review; resolve Critical/Important findings. Final packet review follows its commit/PR binding.
- [ ] Commit approved implementation scope, normal push, one connector PR attempt.
- [ ] If PR exists, inspect new exact-head CI; otherwise provide the specified 403 handoff.
