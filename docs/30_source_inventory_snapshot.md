# Bootstrap source-inventory snapshot

The archive seed described a 2026-08-27 GitHub repository containing only MIT `LICENSE` and a one-line `README.md`, plus the external folder hierarchy, `Base15-26.xlsx`, and reports 260-269. Live M0 verification confirmed the listed filenames and hierarchy but found no prior source hashes, sizes, or page counts in the seed, so byte-level continuity cannot be inferred from it. The verified hash/page/text baseline and explicit differences are recorded in `docs/live_resource_inventory.md`.
