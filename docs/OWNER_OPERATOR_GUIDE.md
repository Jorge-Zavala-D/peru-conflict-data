# Owner-assisted setup collector: source-only operator guide

> OFFLINE/SYNTHETIC ONLY. The procedure below describes a future authorized
> deployment. Offline completion is not live admission or isolation acceptance.

**Production is closed. These are future procedure instructions, not executable
setup authority.** No real grant, cooperative policy, custody sink or account
session is installed. All seven real decisions remain unresolved and the 108
real checks remain NOT RUN. This package does not approve a Dropbox app or scopes.

## Offline developer demonstration

From the frozen development environment:

```
uv run python scripts/prepare_m2_setup_work_order.py offline-demo
```

The command uses synthetic identities and API-shaped fake HTTP responses. It
creates only disposable local evidence, records intentions before dispatch,
imports captured bytes against a separate fixture witness, maintains separate
checkpoint records. It consumes only admission-owned work orders, with account
re-observation at actor handoffs and prerequisite-bounded dispatch deadlines. Sharing
acknowledgements persist job identities and permit bounded status observations
without repeating mutations. Actor mount identity is verified before dependent
work. The two actual publication component controls have separate durable receipts.
Cleanup requires receipt ownership plus fresh metadata, exact-byte and ancestry
observations, followed by a file-ID/revision conditional request.
Its output is OFFLINE_SYNTHETIC, never real isolation acceptance. Test concurrency
and concealment assumptions do not establish Dropbox guarantees or owner policy.
The other commands fail before opening private evidence or looking up credentials.

## Future owner/operator procedure — separately authorized only

1. The owner separately accepts the custody/recovery/retention procedure and exact
   setup/testing authority, with bounded session validity and one-use scope. A
   reviewed independent M2 authority source must bind the immutable V3 proposal,
   implementation, private sink, checkpoint and opaque private binding references.
   Do not edit V3 nulls or its false authorization flags. Strict parent-CAS mode
   remains blocked by Dropbox's missing parent precondition; a cooperative
   exclusion-of-concurrent-writers policy requires separate review and acceptance.
2. Coordinator, A and B sign in independently on approved devices through a later
   authorized collector. Never send credentials, names, emails or person-account
   mappings through this interface, chat, Git or synchronized technical outputs.
   Private binding verification must establish the account, namespace and actual
   actor mount. A/B do not need access to the coordinator's unshared root.
3. Release exactly one scoped work order after durable intent publication. The
   future approved transport or supervised operator performs only that operation.
   Capture original secret-free request/response evidence before normalization.
   A transferred capture requires a separate owner-witnessed expected digest bound
   to the original order/session; its own uploaded hash is not authentication.
4. Import evidence, retain every error/uncertain result, and check the independent
   use checkpoint before continuing. An interruption is UNKNOWN, not a retry.
   Contradictory denial requires reconciliation and does not grant cleanup rights.
   Pending invitation/share conversion is not active membership or acceptance.
5. Cleanup is only for an exactly receipt-owned file with unchanged ID, revision,
   bytes and permitted ancestry, using ID plus file revision as the server-side
   delete condition. Never adopt replacement objects, accept new revisions merely
   because bytes match, delete folders, or automatically revoke permissions.
6. Owner acceptance of real isolation comes later and remains separate from
   production-environment approval, prospective issuance, delivery acceptance and
   final annotation-launch authority. The two local component controls do not
   establish live accepted-storage coverage or authorize annotation locking.

## Limitations

Schema consistency is not real event authentication. Hashes are not authority,
identity, task ownership or universal rollback protection. Restoring both a run
and its independently maintained checkpoint defeats local rollback detection
under a malicious trusted-administrator model. Manual procedures cannot prevent
a person acting outside them. Missing scopes, invalid tokens, wrong roots,
unknown locators, ambiguous not-found responses and incomplete link/member
visibility must stop as inconclusive, not become a successful denial by choice.

The only next owner action after a validated source push is source PR review or,
if normal PR creation is unavailable, creation from the supplied pushed comparison.
Use the accompanying handoff for actual validation/publication status. No live
preparation, custody collection, account sign-in or operational approval follows
from this guide.
