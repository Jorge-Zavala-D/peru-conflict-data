# Required. Point to the external Defensoria data root; never commit a local value.
CONFLICT_DATA_ROOT=

# OCR is opt-in after page-level PDF forensics and benchmark justification.
CONFLICT_OCR_ENABLED=false
CONFLICT_LOG_LEVEL=INFO
